services:
  - type: web
    name: rfp-agent-ai
    env: node
    plan: free
    region: oregon

    buildCommand: |
      npm install
      npm run build
      ls -la dist

    startCommand: npm start

    envVars:
      - key: NODE_ENV
        value: production
